services:
  - type: web
    name: production-player-cloud-beta
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /health
    envVars:
      - key: DESKTOP_SECRET
        generateValue: true
      - key: PUBLIC_BASE_URL
        sync: false
